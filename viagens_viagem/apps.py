from django.apps import AppConfig


class ViagensViagemConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "viagens_viagem"
    verbose_name = "Viagens — viagens (agrupador)"
